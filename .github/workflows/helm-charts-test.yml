name: Helm Charts

on:
  workflow_call:

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint and Test Helm Charts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Set up ko
        uses: ko-build/setup-ko@d006021bd0c28d1ce33a07e7943d48b079944c8d # v0.9

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1
        with:
          version: v3.14.0

      - name: Set up chart-testing
        uses: helm/chart-testing-action@6ec842c01de15ebb84c8627d2744a0c2f2755c9f # v2.8.0

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run helm-docs
        run: task helm-docs

      - name: Check for uncommitted changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Error: helm-docs generated changes that are not committed"
            git diff
            exit 1
          fi

      - name: Run chart-testing (lint)
        run: ct lint --config ct.yaml

      - name: Create KIND cluster
        uses: helm/kind-action@92086f6be054225fa813e0a4b13787fc9088faab # v1.13.0

      - name: Build and load image into KIND
        run: |
          # Build to local Docker daemon, then load into KIND
          KO_DOCKER_REPO=ko.local ko build ./cmd/thv-registry-api \
            --base-import-paths \
            --tags=ci-test \
            --platform=linux/amd64

          # Load the image into the KIND cluster
          kind load docker-image ko.local/thv-registry-api:ci-test --name chart-testing

      - name: Deploy PostgreSQL for testing
        run: |
          # Create the test namespace (must match ct.yaml namespace)
          kubectl create namespace toolhive-test

          # Deploy PostgreSQL using raw manifests to the same namespace
          kubectl apply -f .github/postgres-manifests.yaml -n toolhive-test

          # Wait for PostgreSQL to be ready
          kubectl wait --for=condition=available deployment/postgres -n toolhive-test --timeout=120s
          kubectl wait --for=condition=ready pod -l app=postgres -n toolhive-test --timeout=120s

      - name: Run chart-testing (install)
        run: ct install --config ct.yaml
